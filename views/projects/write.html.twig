{% extends 'shared/layout.html.twig' %}
{% block title %}New project{% endblock %}

{% block body %}

    <!-- page-banner-section
        ================================================== -->
    <section class="page-banner-section">
        <div class="container-fluid">
            <h1>
                {{ mode == "edit" ? "Edit" : "New" }}&nbsp;project
            </h1>
        </div>
    </section>
    <!-- End page-banner section -->

    <div class="container">
        <div class="row">
            <div class="col-lg-7">
                <div class="text-left">
                    <p></p>
                    <form method="post">
                        {% if model %}
                            <input name="id" hidden value="{{ model.id }}" type="text">
                        {% endif %}
                        <div class="form-group">
                            <label class="small ">Title</label>
                            <input value="{{ model.title }}" name="title" type="text" class="form-control" placeholder="Title">
                        </div>
                        <div class="form-group">
                            <label class="small ">Subtitle</label>
                            <input value="{{ model.subtitle }}" name="subtitle" type="text" class="form-control" placeholder="Subtitle">
                        </div>
                        <div class="form-group">
                            <label class="small ">Description</label>
                            <textarea name="description" type="text" class="form-control"
                                      placeholder="Description">{{ model.description }}</textarea>
                        </div>
                        <button type="submit" class="btn btn-primary my-1">Save project</button>
                        <button type="reset" class="btn m-1 btn-default">Reset</button>
                        <a href="{{ request.previousUrl }}" class="btn btn-dark my-1">Cancel</a>
                    </form>
                </div>
            </div>
        </div>
    </div>
{% endblock %}